"use client"
import { useState } from "react"
import { useRouter } from "next/navigation"
import Link from "next/link"
import { useSession } from "@/lib/session"
import { register } from "@/lib/auth"
export default function RegisterPage(){const router=useRouter();const{login}=useSession();const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState<string|null>(null);const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError(null);setLoading(true);const result=await register(name,email,password);if(!result.ok){setError(result.error??"Не удалось зарегистрироваться");setLoading(false);return}const loginResult=await login(email,password);setLoading(false);if(!loginResult.ok){setError(loginResult.error??"Аккаунт создан, но вход не удался");router.push("/auth/login");return}router.push("/");router.refresh()}return <div className="form-page"><div className="page-kicker">KASHRA</div><h1 className="form-title">Создать аккаунт</h1><form className="form-box" onSubmit={submit}><div className="form-field"><label className="form-label">Имя</label><input className="form-input" value={name} onChange={e=>setName(e.target.value)} required placeholder="Ваше имя"/></div><div className="form-field"><label className="form-label">Email</label><input className="form-input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="you@example.com"/></div><div className="form-field"><label className="form-label">Пароль</label><input className="form-input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8} placeholder="Минимум 8 символов"/></div>{error&&<div className="form-error">{error}</div>}<button className="buy-btn" style={{width:"100%"}} disabled={loading}>{loading?"Создаём…":"Зарегистрироваться"}</button></form><div className="form-note">Уже есть аккаунт? <Link href="/auth/login">Войти</Link></div></div>}