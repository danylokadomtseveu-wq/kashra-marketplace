"use client"
import { useState } from "react"
import { useRouter } from "next/navigation"
export function ProductActions({product}:{product:{id:string;title:string;price:string}}){const[added,setAdded]=useState(false);const router=useRouter();function add(){const current=JSON.parse(localStorage.getItem("kashra-cart")??"[]") as Array<{id:string;title:string;price:string;qty:number}>;const existing=current.find(x=>x.id===product.id);if(existing)existing.qty+=1;else current.push({id:product.id,title:product.title,price:product.price,qty:1});localStorage.setItem("kashra-cart",JSON.stringify(current));setAdded(true)}function buy(){add();router.push("/cart")}return <div className="product-actions"><button className="buy-btn" type="button" onClick={buy}>Купить</button><button className="header-btn btn-ghost" type="button" onClick={add}>{added?"В корзине":"В корзину"}</button></div>}
